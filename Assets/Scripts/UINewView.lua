--!Type(UI)

--!Bind
-- local myButton : UIButton = nil

-- -- Check if myButton is properly bound
-- if myButton ~= nil then
--     -- Function to be called when the button is clicked
--     local function onClick()
--         print("Button clicked!")
--         -- Add your custom functionality here
--     end

--     -- Check if the onClick event exists for myButton
--     if myButton.onClick ~= nil then
--         -- Add listener to the onClick event
--         myButton.onClick:AddListener(onClick)
--     else
--         print("Error: onClick event not found for myButton.")
--     end
-- else
--     print("Error: myButton is not properly bound.")
-- end
